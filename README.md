# Simple script to setup fedora to my preference
```sudo bash ./setup.sh```  
Optional flags:  
```-c``` includes ccache setup in ~/.bashrc  
~~```-g``` installs grub2 theme~~  
```-os``` another os for software install (```fedora```(default) uses dnf, ```ubuntu``` uses apt)  